import React from 'react';
import logo from '../../src/img/logo.png';

const Header = () => {
  return (
    <div className='center'>
      <img src={logo} alt='' />
    </div>
  );
};

export default Header;
